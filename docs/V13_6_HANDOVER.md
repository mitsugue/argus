## 2026-09-15 進行条件更新と保存照合の継続

オーナーは13.6修復後の13.7着手・完成までの続行を明示的に承認した。[13.7要件](V13_7_REQUIREMENTS.md)を適用し、以前の13.7保留を解除する。13.6修復とは変更を分け、無関係な残課題で独立改善を止めない。名称廃止と履歴・権限保護を継続する。

13.6.4はmain `23edf9535702788120d171947850187282432188`でREADY。ブラウザの自動更新、Todayの見立て・根拠・比較・SQの読み込み表示と取得後の停止、4層チャート、Seven Signを確認した。実配布JS/CSSの共通アニメーションと名称検査も合格。ニュース40/40件解析済み、新しいGPT説明は数値不整合の初回応答を棄却し修正後を採用・ローカル保存した。費用制限無効・write-through・起動時12行復元も確認した。

Pages34876599435の配布・identityは成功したが、保存照合が900秒で時間切れ。先行チェックポイントは18:01:09UTC完了し、11/12件を18:03:12までに取得、最後のQQQ20Dが読込み途中だった。同じtriggerIdで再開し二重生成は行わない。13.6.5で観測上限を1800秒、当該jobを45分へ整合し、内容・12件の集合・鮮度・同じbuild/triggerの検証は変更しない。長い観測時間を復旧時間やRPOの達成証明とは扱わない。仮想時間で1000秒後の全件収束と単一POSTを確認し、既存の失敗・不一致拒否検査を維持した。

## 2026-09-15 詳細画面の読み込み状態（13.6.5候補）

- 主要画面の共通アニメーションはPR #372、main `23edf9535702788120d171947850187282432188`へ必須検査・Product証明通過後に通常マージ。13.6.4の配信確認を継続中。
- 銘柄検索、調査結果、相談資料の取得、採点・銘柄同期状態、旧バックアップの読込みにも同じTriangleStepLoaderを接続する。保存済みの表示を維持し、通信失敗・未設定と処理中を区別する。
- 銘柄を切り替えた後に古い検索や調査応答が新しい対象へ反映されないようにする。公開読取りは12秒の上限を持つ。計算・判定・保有データ形式は変更しない。
- 390pxの隔離実ブラウザで検索中・旧応答の拒否・取得失敗時の停止・再取得成功を確認。TypeScript、ビルド、51件の版・配信整合検査、ソースと配布物の外部名称ポリシー検査は合格。実機・本番での追加箇所の確認は配信後に行う。
- 本番のGitHub未認証読取りは403（残0/60）、同じインスタンスの既存認証による読取りは200（残4804/5000）だった。PR #373は既存認証を復旧参照へ接続し、対象リポジトリの参照・コミット・比較だけに送信先を限定する。認証値は証跡へ記録しない。
- iPhoneミラーリングは端末使用により切断。実機の新しい保存点の読戻し・復号照合と通知バナー・遷移は未確認。予測力・EPS・自然定期処理の未達も維持する。13.7は保留。

## 2026-09-15 読み込み状態の仕上げ（13.6.4候補）

- オーナー指定の既存TriangleStepLoaderを再利用。見立て、個別銘柄、ニュース、比較チャート、SQ日程、対話・履歴、費用・通知・暗号化保存・公開診断の処理状態に接続した。前回データを保持する更新と初回取得を区別する。
- ニュースの再取得中は記事を残したまま読み込み状態を知らせる。未設定、取得失敗、データなし、待機中を動作中に見せない。省アニメーション設定と読み上げを維持する。
- 実装・frontend lint・ビルド済み。390px幅の隔離ブラウザで各読込箇所、失敗時の停止、横溢れなし、省アニメーション設定を確認。既存CSSの優先順位不良を修正した。ニュースの遅延・失敗・復帰時の記事保持も確認。本番確認済みとはまだ扱わない。
- 13.6.3のiPhone保存修正はPR #371、main ee265fdbbd872c217a871aa0b827f97cf01c2328にマージ済み。iPhoneでの暗号化保存・読戻しは配信後の確認待ち。
- 13.6.2本番ブラウザで版表示・Today・個別銘柄を確認。Todayに統合AI、実際の比較チャート、7条件、重大記事2件、SQ予定が表示された。全機能の受入完了とは別。
- Recovery修正はPR #370で継続。13.6全体の本番受入後に総点検して停止し、13.7は明示再開まで保留する。

## 2026-09-15 iPhone実機・13.6.2配信・13.6.3保存修正

- PR #369は必須CI、Product証明、premerge、release-checks合格後に通常マージ。
  main `bbb21d744ff9fb65cad203d0b64590d942e8ef45`、製品13.6.2。
  ニュースの同一出来事の続報をまとめ、原記事・訂正は保持する。
  本番識別manifestの配信は成功。Pagesの配信・iPhone更新は確認中。
- iPhoneホーム画面版13.6.0のSettingsでWeb Push登録を実行。
  サーバー受付に加え、端末の表示処理の報告を
  2026-09-15 01:10:40 / 01:13:39 JSTに実画面で確認。
  アプリを閉じて送信したが、通知バナーの目視と通知から開いた記録は未確認。
- 同端末の自動保存は停止中だった。既存の接続情報で有効化すると
  `JSON Parse error: Unexpected identifier "en"` が発生した。
  現行の言語設定は非JSON文字列、バックアップは全キーをJSONとしていたため。
  元の保有銘柄・設定・判断履歴の削除やリセットは行っていない。
- 13.6.3候補は言語設定の実保存形式と旧JSON文字列の両方を読み、
  復元時は現行設定と同じ形式へ戻す。破損したJSON文書は引き続き保存を停止。
  実際の言語設定関数を使う回帰検査、暗号化復元、保存保護、51件の版・配信検査に合格。
  ビルド成功。実機の保存・遠隔読み戻し・復号照合は配信後に再確認が必要。
- Watchtower `34865264082` の遠隔保存はバックエンドで最終成功したが、
  4分の観測制限を超えて903秒かかったためActionsは失敗。
  PR #370で受領処理の保存優先と有界な観測時間を修正。96件の関連検査に合格。
  最新mainへ追従し古い候補の不要CIを取消済み。実配信・自然EC2実行は未確認。
- 13.6の受入は未完了。既存の独立予測検証、EPS取得、自然定期処理などの未達を維持。
  13.7には着手しない。

# v13.6.0 引き継ぎ文書（13.5 系の到達点・2026-09-08）

## 2026-09-15 全機能点検・13.6.2候補

- 本節を過去のモデル・予算・進行状況より優先する。13.7は未着手のまま保留する。
- 予測台帳の2件の失敗はAI応答待ち240秒のタイムアウトだった。PR #367で420秒へ
  修正し、AI失敗時にも既存の正本記録・採点を継続する。実行34862047027は成功し、
  正本記録、採点、台帳コミット、遠隔保存まで通過した。
- PR #368は通常マージ済み。バックエンド13.6.1 /
  `d8a58205fbfd6b7da673dab37f8db050e493c04d`のhealth/ready両方を実測した。
  旧インスタンスでは起動時の履歴照合が失敗したが、同じコードで217コミットの
  親子関係照合が成功し、新インスタンスは保存状態を復元してREADYになった。
  一時的な通信失敗の詳細分類・再試行の扱いは再発確認対象とする。
- 再起動で残っていたAIの未精算予約12件を解放し、永続費用台帳の復元・書込みを確認。
  解放を実課金ゼロの証明とは扱わない。実料金との照合は別途必要。
  費用による実行停止は解除中。主モデル設定はgpt-6-astra、Geminiは補助。
  cost-policyの表示集計は直近500件が対象のため、完全な日次・月次総額とは区別する。
- Watchtowerの実行識別情報からbackendVersionが消える不具合を修正した。
  配信後の実行34865264082で実処理の確認を継続する。
  caos-scanは再有効化済みだが、自然スケジュール実行は未確認。
  EC2由来Watchtowerの定期呼出しも未確認で、手動実行の成功で代替しない。
- #367はsquash mergeのためPages配信が失敗した。Recoveryのみの#368はPagesを
  起動しない仕様であり、フロントはまだ13.6.0。次の製品修正は13.6.2で通常マージする。
- 実ブラウザでToday、セブンサイン、実データの過去比較線、ニュース解析結果、
  15銘柄の価格、個別銘柄の市場比較を確認。未認証ブラウザの個別AI対話は未確認。
  本候補では同一episodeIdの速報・続報を一つのカードにまとめ、関連原文を展開可能にする。
  元記事・解析・保存データは保持する。銘柄カードの未説明の確度割合を状態文言へ変更する。
- iPhoneは接続・13.6.0表示を確認した。端末移行の保存/読み戻し、更新後の全画面、
  新しいプッシュ通知の実受信は未受入。
- 指数EPS/PER原典の403、独立期間での予測力検証、米国個別株の一部AI入力欠測は残件。
  データ取得・画面表示・予測力検証は別の達成状態。BUYは無効を維持する。


## 2026-09-14 13.5.72本番確認と13.5.73候補

- 13.5.72 / `c16bae61572c78434882191e53ff4e28af85cde5` を通常マージで配信。
  必須CIと候補証明を確認。新しい本番インスタンスのREADYを確認した。
- 私的対話の旧回答2件を暗号化遠隔保存から実際に復旧し、既存の採用済み回答の
  内容ハッシュ一致と生成可能状態を確認した。71で発生したTypeErrorは解消した。
- 72で新しい質問へのGPT実応答を確認したが、根拠にない数字、その後の出力形式違反で
  不採用となった。生成成功・採用・保存は別の状態であり、新回答の受入は未完了。
- 公開履歴の同じGitオブジェクトの検証付き読取りは実データ11チャンク・13.16秒で成功。
  26件の遠隔履歴の整合性を確認したが、ローカル追加分を含む同期は時間不足で未完了。
- 73候補では私的対話の文字数・根拠数・出力キーの指示を明示し、形式違反にも既存の
  一度だけの修正を適用する。採用条件は変更しない。公開履歴同期の総時間上限を180秒とし、
  個々の通信上限・サイズ・ハッシュ・CAS・不変履歴を保つ。無制限再試行はしない。
- 前回のAI説明を表示する際も、同じ記事ID・改訂・処理時点のニュースだけ重複を除く。
  続報・訂正・説明後の更新は残す。保存原本は変更しない。
- 13.5/13.6は未完了。端末移行・実機通知、指数EPSの取得、独立期間での予測力検証、
  全画面・定期処理の受入は残る。13.7は明示的な再開指示まで保留する。

> **現行の範囲・停止条件（2026-09-09更新）**：名称変更と13.5残件を本番確認まで完了し、
> その完成版から13.6へ進む。**13.6の本番受入で停止**する。13.7の調査・実装・試作・PR・配信は、
> 来週以降のオーナーによる明示的な再開指示まで禁止。日付の到来や13.6完了では自動着手しない。
> 現行の詳細要件は [v13.6製品要件・受入台帳](V13_6_REQUIREMENTS.md)。
> 日本株分析の改善と実データの過去チャート重ね描きを13.6初期工程に置く。
> 以下の旧時点の記録と現況が異なる場合は、最新の13.5検証台帳を優先する。


v13.6.0（統合 AI と画面リニューアル、Codex + Astra）に引き継ぐための一枚。13.5 系は
「実データを取得・保存・判断・端末表示する」ことを本番で通し、判断の勝手な合格や見えない
警告を排した状態で終える。新しい統合 AI・新予測エンジンはここには入っていない。

> 2026-09-09 Codex開始後の更新は [13.5系検証台帳](V13_5_CODEX_STATUS.md) を参照。
> 立花認証と海外フローは開始時点で復旧を確認。以下の旧記録を現在の障害と取り違えない。

## 1. 本番の識別情報（この文書の時点）

| 面 | 識別子 | 確認方法 |
|---|---|---|
| バックエンド | Render `argus-backend-3j2m`、`/healthz` の `backendVersion` / `buildSha` | `curl https://argus-backend-3j2m.onrender.com/healthz` |
| フロント | GitHub Pages `https://mitsugue.github.io/argus/`、`__ARGUS_VERSION__` | ページソースの先頭 8KB |
| 製品バージョン | `product-version.json` / `backend-version.json` / `web/package.json` の三点同期 | `test_argus_deploy_scope.py::test_no_stale_version_pin_survives_a_bump` |
| 配信経路 | 製品 PR → `deploy-pages.yml`（証明書・readiness・acceptance）／Recovery PR → Render 自動デプロイ + `backend-warm-after-deploy.yml` | Actions |

到達バージョンと確認結果は末尾「7. 完了時の本番確認」に記す。

## 2. 未解決事項（v13.6.0 で扱うもの）

1. **BUY は構造的に無効のまま。** 反転軸 BUY の検証は 2026-09-07 に FAIL
   （`docs/REVERSAL_BUY_VALIDATION.md`）。有効化には同ドキュメント §7 の計画（条件細分化、
   サンプル延長、モメンタム基準、月次 3 回 PASS）とコードレビューでのレジストリ固定が必要。
2. **予測は「類似局面の頻度」で、検証済み確率ではない。** walk-forward BSS ≈ −0.003〜+0.003。
   `probability-eligibility-v1` の全条件を満たすまで確率表示はしない。
3. **信用需給（二市場信用残）は週次の公式 xls を取り込む運用。** `jpx-credit-weekly.yml`
   （水・木 19:30 JST）が JPX の週次ファイルを取得し Market Ledger に入れる。未公表週は
   gap として報告するのみ。2026-07-17 / 07-24 は JPX 側に公表ファイルが無い。
4. **ニュース取込は 1 プロセス内の逐次処理。** 読み出しはロックを待たない（Recovery PR-7、backend d614c30d）が、
   1 サイクル内の AI 解析は逐次で、バックフィル時は数十分かかる。並列化は未実施。
5. **Recovery ゲート（checkpoint-v2）の測定契約。** allocator の絶対上限は「使用中バイト」と
   「復元元サイズ相対」に分けた（`docs/checkpoint-v2-mapping-attribution.md` v13.5.64）。
   本番スナップショットが今後さらに大きくなれば `allocatorAnonymousBytes`（256 MiB）に
   当たる可能性がある。上限を触る前に同ドキュメントの実測表を更新すること。
6. **Recovery マージは Pages を走らせない。** 証明書を持たないため。代わりに
   `backend-warm-after-deploy.yml` が Render の反映を待ってウォームする。
7. **コスト方針の使用台帳は永続ルートへ write-through**（Recovery PR-7、backend d614c30d）。
   ジャーナルスナップショットが古くても再デプロイ後に union 復元する。並行実行は予約行で防ぐ。
   write-through は AI 実行の予約・確定・スキップ記録の時点で書くため、再起動後に AI 試行が
   一度も無いうちは `ledgerDurability.lastPersistAt` が null のまま（実行回数はジャーナル側で保持）。
8. **Tachibana（立花）ライブは読み取り専用シャドー。** 本番の秘密鍵ファイルの形式問題
   （`AUTH_KEY_PARSE_FAILED`）はオーナー側の再アップロード待ち。
9. **EC2 ブリッジ（moomoo）は US のみ。** JP はブリッジ対象外（J-Quants / Tachibana）。
10. **GitHub の schedule 起動が遅延・欠落する。** 2026-09-08 は macro-event-analysis の 00:35Z / 02:35Z が
    起動せず、market-watch は 02:00Z が 02:09Z 起動。定期実行の証跡は起動した枠で取る。生成の
    追跡は `generateRun`（running/done/failed/interrupted）で、workflow 側のタイムアウト（600 s）後も残る。
11. **market-watch の intel-collect がコールド起動直後に 90 s で時間切れになる。** 暖機
    （`backend-warm-after-deploy.yml`）が先に走れば起きない。恒久策は v13.6.0 側で検討。
12. **製品全体で人名由来の名称・識別子を廃止する。** 総称は「日本株分析エンジン」/
    `jp_market_engine`。API・保存データ・配布物を含む移行と追加検証候補は
    [正式要件](JP_MARKET_ENGINE_REQUIREMENTS.md)を参照。表示だけの修正では完了としない。

## 3. 利用中のデータ源

| データ | 供給元 | 頻度 / 遅延 | 状態の見方 |
|---|---|---|---|
| 日本株 日足・週足 | J-Quants V2（Standard、`JQUANTS_API_KEY`） | 日次、16:30 JST 更新、完了セッション後 15 分再確認 | `decision-evidence` の `marketTruth.status` |
| 米国株 日足 | Twelve Data（BASIC、9 銘柄 warm、閉場時 6 時間ごと cold fill） | 日次 | `data-quality/status` |
| 指数 ^N225 / ^VIX / SPY | Yahoo v8 chart（OHLCV、PIT 付与） | 日次 | `marketView.sourceStatus` |
| 二市場信用残（D01） | JPX 公式週次 xls → `ops/imports/*.csv`（〜2026-07-10）+ Market Ledger 取込（それ以降） | 週次、金曜締め・水曜公表 | Today「方式と根拠の詳細」の 信用残 行 |
| 1570 信用倍率（D02） | J-Quants 週次 | 週次 | `marketView.projection.families.D02` |
| 海外投資家フロー（D05） | 台帳（現在 missing） | 週次 | `sourceStatus.foreignFlow` |
| VIX（D06） | Yahoo ^VIX（FRED は鍵未設定） | 日次 | `sourceIssues` |
| 決算（D07） | J-Quants fins/summary | 随時 | `families.D07` |
| 為替・金利 | `/api/argus/rates`（FRED / Yahoo） | 日次 | Today MACRO 行 |
| イベント台帳 | `argus_important_events`（BLS/FOMC/BOJ/財務省入札など、31 日先） | 2 時間ごと更新 | `/api/argus/important-events` |
| ニュース | Gmail 購読メール（日経・BOJ・OFAC 等、認証送信元のみ） | 75 秒ごと取込 | `/api/argus/news-intake/health` |
| 仮想通貨 | CoinGecko（SYMBOL_TO_COINGECKO） | 準リアルタイム | Holdings のバッジ |
| 投信 | 投信総合ライブラリー | 日次 | 同上 |

## 4. 利用中の AI モデルと予算

| 用途 | モデル（既定） | 環境変数 | 予算・上限 |
|---|---|---|---|
| イベント事前/事後シナリオ | `gpt-6-astra`（不可時のみ `gpt-5.6-terra` に代替、両方を記録） | `ARGUS_OPENAI_MODEL_EVENT` / `_FALLBACK` | 1 日 6 回、$0.08/回見積、イベント枠予備 $0.50 |
| ニュース解析（news_intel） | `gpt-5.6-terra`（難案件のみ `gpt-5.6-sol` へ 1 回昇格） | `OPENAI_MODEL` / `ARGUS_OPENAI_SOL_MODEL` | SCHEDULED 日次 $2.00 − 予備 $0.50 |
| 見出し翻訳 | Gemini（`GEMINI_API_KEY`） | — | 同上の枠 |
| 単価（公式・2026-09-07 参照） | astra $10/$1(cached)/$50、terra $2/$0.2/$12、sol $4/$0.4/$20 per 1M | `_AI_PRICING` | `OPENAI_PRICE_*` で上書き可 |
| 全体 | 日次 $5、月次 $80、緊急予備 $2（推定値のハード停止） | `AI_DAILY_BUDGET_USD` 等 | `/api/argus/ai-cost`（admin） |

方針モード: `SCHEDULED_AI`（`ARGUS_EVENT_AI_OPT_IN=1`）。公開ステータス
`/api/argus/cost-policy` に 鍵の有無・枠予算・実行回数・直近実行・直近見送り理由・台帳の
耐久性 を分けて出す。

## 5. 既存の判断仕様（SDA v2、変更なし）

- 5 アクション: BUY / HOLD / WAIT / REDUCE / EXIT。BUY は ①リスク制約なし ②需給・トレンドの
  反転状態が反転初期・自律反発・回復試験・上昇確認のいずれかで **検証済み** ③検証済み買い成立
  レジストリ（`VERIFIED_JP_MARKET_ENGINE_BUY_ARTIFACTS`、現在は空）の本番採用 ④保有側の追加許可。
- 確度 = min(基準値 {BUY 70, HOLD 60, WAIT 45, REDUCE 70, EXIT 80}%, riskKernel 上限)。
  データ不足時は 25% 固定。
- REDUCE は保有の含み損 −25% 以下（`positionExposure`）で `REDUCE_RISK`。
- WAIT の 3 種: データ不足（評価未了）／リスク制約／買い条件不成立。
- 判断の正本は `decision-evidence`（8 銘柄/要求、端末側は 8 件ずつ全登録銘柄）。
- MARKET SIGNALS（7 条件、日本固有）は市場観であり行動権限を持たない
  （`actionAuthority=false`）。米国選択時は「日本市場の値」と明示。

## 6. 既存の予測仕様（変更なし）

- チャート予測: `argus_today_intelligence`（today-replay-calibration-v3-market-conditioned）。
  10 年日足の類似局面 kNN（trend20 / momentum5 / atrPct / closeLocation / volumeRatio）に、
  日本は 信用倍率・売り残高・VIX 水準・VIX 10 日変化・対 SPY 相対力、米国は VIX 水準・
  VIX 10 日変化・対 SPY 相対力 で条件付け（知識ラグ付き PIT 結合、結合窓 信用 45 日・VIX 10 日）。
  出力は 1/5/20 営業日先の方向の **出現頻度**、ATR14 帯、支持抵抗。
- 反転/下方軸: `jp_market_engine.build_reversal_engine`（^N225 と ^VIX の MACD/SAR/BB/RSI）、
  状態 REVERSAL_EARLY / TECHNICAL_REBOUND / RECOVERY_TEST / CONFIRMED_ADVANCE / FALSE_RALLY /
  MIXED。行動権限なし。
- 対応表: `docs/forecast-method-jp-us.md`。BUY 検証: `docs/REVERSAL_BUY_VALIDATION.md`。

## 7. 完了時の本番確認（記入）

完了報告の各項目（配信識別子・実画面・定期実行での生成→保存→表示・再デプロイ後の台帳・
取込中のニュース応答）は最終報告に記載し、ここには識別子のみを残す。

- バックエンド: Render `argus-backend-3j2m` = 13.5.66 `87bf4ec2`（2026-09-08 04:0xZ 反映、PR #309）。
  直前の Recovery PR-7 は 13.5.65 `d614c30d`（02:58Z）。
- フロント: GitHub Pages = 13.5.66（`__ARGUS_VERSION__="13.5.66"`、バンドル `assets/index-Bu-stlo0.js`、
  Pages run 34185049990: scope → acceptance-runtime-admission → build → readiness → deploy →
  candidate-identity → business-snapshot-trigger → seed-warm-profile → business-snapshot-acceptance →
  public-acceptance が全て success、04:08Z 完了）。
- 本番の信用需給結合（13.5.66、04:07Z）: 1306/1321 は `credit` joined（periodEnd 2026-08-28、
  availableFrom 2026-09-02、age 10 d / 45 d）、`vix` 9/3、`us` 9/4。QQQ は credit not_applicable、
  SPY は credit / us not_applicable。特徴量に `creditRatio` `creditShortTn` が復帰。

## 2026-09-12 旧AI費用集計の復元修正（13.5残件）

旧 `/api/argus/ai-cost` は中核の `cost-policy` と別のメモリ内集計であり、
復元に必要なimportの欠落と例外の無視、復元前に発生した利用分を落とすmax合成、
日次ワークフロー前の再起動で記録を失う問題があった。

`costPolicy.legacyAiCost` に `argus-ai-cost-accounting-v1` を保存する。
JSTの日別・起動単位の単調な累計を、既存の費用ファイルの原子的保存と
暗号化チェックポイントへ接続する。同じ起動の古いスナップショットは前方の
累計へ統合し、独立した起動分は加算する。直近20/50件の表示枠は累計に影響しない。
新版の保護APIにも同じ保存形式を含め、日次ワークフロー経由の再取込みで重複計上しない。

旧形式の保存済み月次・日次総額は、報告値と出典ハッシュを保持する。
移行前の全履歴を再構成した総額とは表示しない。不明な過去のトークン・モデル・
重複関係を推定で補わない。旧集計と中核の費用台帳を単純加算しない。
旧形式の月次読取りは、保存済みの現行月と再取込み対象が新形式へ移行したことを
確認するまで必要であり、撤去時には別途移行証跡を残す。旧版へ戻す場合も新形式を
保持できる版を使い、最新の費用保存原本を保全する。

この項目は13.5の保存・復元修正で、13.7のコスト最適化ではない。
13.6の機能別トークン・実応答モデル等の全呼出し履歴は別途接続・受入が必要。
本修正の実装・検査と本番配信・再起動後の確認は区別して受入台帳に記録する。
13.6本番受入後の全機能総点検・不具合の再検証までで停止し、13.7は明示再開まで保留する。

日次公開より新しい更新前原本は、既存の永続ルートの
`ai_cost_migration_baseline.json` として別途保全し、起動時だけ取り込める。
同一内容の再取込みは冪等で、新しい累計を上書きしない。原本の数値と出典ハッシュを
確認して配置し、通常チェックポイントへの保存・読戻し後も元の証跡は保持する。
移行用ファイルの破損は表示するが、有効な中核台帳の復元を妨げない。

## 2026-09-14 期間別の説明と費用判定の追加修正

PR #353 は main `61dc5b6e19b10f9a11ef7192ffdc9cdae00ac8c7` にマージされ、
Pages 34802660892 の本番受入まで成功した。別起動後に私的対話1件の
暗号化遠隔保存からの復元と内容ダイジェストの一致を確認した。その回答自体は
根拠にない数値で不採用になった記録であり、回答成功やiPhone移行とは区別する。
PR #355 は `545a1643d44389d41cca638b22a0a56ae6ddd188` にマージした。
回答の一度だけの修正要求と13.5.69の配信は別途本番確認する。

13.5.70の変更では、費用上限解除時も残っていた1要求12,000トークンの
費用判定を解除する。認証、用途、重複抑制、入力の有界性と費用記録は維持する。
拡張した市場説明が本番で `event_token_limit` により未実行だったことが修正理由。
新しい市場説明の実応答・保存・画面確認までは復旧完了としない。

日経平均の1/5/10/20営業日の対話には、同じ期間の計算根拠ハッシュと一致する
比較チャートを固定して保存する。回答の編集意図はそのチャートにも適用する。
最新データで以前の回答の線を置き換えない。Todayで選択した対象・期間が
公開の日本株5営業日説明と異なる場合は、その対象・期間の所有者向け説明を使う。
所有者接続がない場合は不足を示し、公開5営業日の説明を別の期間の見通しにしない。
既存保存形式の読取りは維持し、新しい自動説明IDだけを版更新する。

取得済み本番データによる4期間の保存・隔離復元、実ブラウザでの4層描画と
携帯幅・デスクトップ幅を確認した。そこで使った回答は検査用であり、
本番GPTの採用回答、予測力、実機通知、EPS取得の本番403解消は未確認。
13.6全体の完成ではない。13.7は引き続き明示再開まで保留する。

### 保存済みの公開説明の表示

13.5.71候補では、公開の保存履歴でも当時のGPTの表示意図、全説明項目、
根拠と比較チャートを再利用する。現在の表示と履歴を明示し、履歴から
現在の文脈で質問を再生成しない。5営業日の比較チャートを重複表示せず、
ほかの期間の計算を開く場合は説明の対象期間と区別する。
選択した説明を読む際は長い一覧を閉じ、再取得なしで一覧へ戻せる。

本番に保存されている実際のGPT回答・計算を取得し、隔離した実ブラウザの
携帯幅とデスクトップ幅で当時の表示IDと全表示要素の一致を確認した。
一覧・記録の読取りは2回、AI生成は0回。循環表示、横方向のはみ出し、
同じ5営業日チャートの二重表示はない。これはローカル表示の確認であり、
13.5.71の本番配信、iPhone受入、予測力の証明とは区別する。

同じ候補で、日経平均比較の公開キャッシュを対象・期間別に端末へ保持する。
起動直後の一時取得失敗で前回の比較線が消えないようにし、取得失敗時は
画面表示中に30秒後、通常は5分間隔で再取得する。共有の同時要求は1件に抑える。
最後の成功時刻と前回表示であることを示し、保有・設定・履歴を削除しない。
128 KiBを超える比較は端末保存せず、利用可能な画面データは保持する。
実データを使ったブラウザで、再読込、取得先の一時503、別期間への切替、
自動再取得による回復を確認した。障害は隔離した模擬応答であり、
本番取得の復旧や実機確認とは区別する。

公開説明の前後に行う遠隔履歴照合には、通信全体の締切判定が未実装だった。
同候補で45秒の経過時間上限を設け、各通信のタイムアウトも残り時間に合わせる。
期限後は新しい読書きを始めず、途中の応答を閉じる。既存の内容照合・不変オブジェクト・
比較交換・隔離復元は維持し、照合失敗を成功扱いにしない。照合失敗後も既存の
ワーカーはローカル履歴と説明更新を継続する。通信終了期限とローカル処理時間は別であり、
遠隔復旧完了の保証にはしない。期限切れ時に履歴が保持されることを関連検査で確認した。
## 13.5.72: 保存通信の整合と新しい画面の読む順序（配信前）

13.5.71後の本番確認で、公開履歴の一部のContents応答にサイズとGitオブジェクトの
不一致を確認した。同じSHAを指定した正規Git Blobs応答はサイズ・Git SHA・保存チャンクの
SHA-256が一致した。内容の切り詰め・変換・既存ファイルの上書きは行わず、不一致時のみ
同じGitオブジェクトを再取得して両方のサイズとハッシュを照合する。全体の待ち時間制限、
チャンク上限、復元前の全件検証とCASを維持する。

71の時間制御変更で、共通クラスを継承する私的対話・端末保存が影響を受けた。
本番の私的対話はTypeErrorで遠隔保存・新規生成が停止し、既存のローカル対話は読める。
継承先の180秒の設定と残時間の返却を修正し、両経路の正常なGET/PUT、書込み認証境界、
期限後の通信停止を検証した。本番復旧は72配信後の確認が必要。

新しい公開Todayの生成では主文を最初に置き、緊急案内だけを短く一つ前に置けるようにする。
他の重要情報は主文の直後で維持し、変更理由から比較チャートへつなぐ。GPTが本文・順序・
構成理由を同じ応答で返し、長い前置きや主文を後回しにする構成は既存の一回の修正対象にする。
当時の保存済み構成は新しい編集条件で失効させず、元のID・文章・計算を保持する。

これらは13.5の保存不具合の修正と13.6の表示要件の実装であり、13.6の本番受入完了ではない。
実機通知、全画面の統合、EPS実接続と改訂履歴、独立検証、総点検は別途未完了。13.7は保留。


### 13.5.74 候補：起動中の保存版表示とイベント結果の整合

13.5.73の本番では、公開44件の遠隔照合、私的対話の既存3件復旧、比較線を参照する新しいGPT回答の初回採用・遠隔5件照合を確認した。前回の表示版と保存版の4層チャートを390/1280pxの実ブラウザで確認。iPhoneの回答表示・通知受信と、新しい全件の次回起動復旧は未確認。13.6全体の受入は未完了。

起動直後に公開APIが説明待ちを返す間、既存の公開履歴を読み、検証できた説明・根拠・計算・表示計画を一組で前回版として表示する。直近3件まで、1回の取得全体12秒以内、公開GETのみ。読み戻せない間は30秒後に再確認し、通常は既存の5分間隔。閲覧済みの前回版を空の応答で消さない。過去の計算を現在の根拠へ移したり、AIを閲覧から起動したりしない。サーバーの300秒表示キャッシュ自体の無効化は別の未変更箇所。

Todayの発表済み案内は、イベント詳細と同じ取得済みデータの正確なID・コード・公式結果を使用する。時間経過の分類だけで「結果待ち」と判定しない。CPIの既存公式結果を再取得・改変せず、結果不明・発表時刻不一致を区別する。

この候補の検査・配信・本番受入は別々に記録する。13.7は明示的な再開指示まで保留。不要な古い候補の検査は取消可能だが、現在の配信に必要な検査は維持する。

配信番号の更新は既存のbackendDeploy対象であるため、この候補はRenderを省略せず通常配信する。配信範囲判定と必須検査を変更しない。

### 13.5.75候補：確定値がそろった期間の市場比較
引け直後に最新日の収集がそろわなくても、指数と比較用ETFの取得済み確定値で同じ期間を比較する。最新完了営業日と比較の終点は別々に保持し、画面と統合AIへ取得待ちを伝える。個別株・業種の欠測を補間せず、未確定の場中価格を終値へ昇格しない。通常の収益率式・価格基準・時点の検査は維持する。53関連検査とTypeScriptは成功、本番反映は未確認。13.6完了や予測力の検証済みとはしない。

### 保存した説明への対話と起動後の接続（13.5.75）
サーバー側で復元した説明が公開GETの合成キャッシュへ直ちに反映されるようにし、個別銘柄の業種分類を実際の銘柄マスターの項目名で参照する。公開GETによるAI実行・データ再取得は追加しない。
所有者の明示質問では、表示した公開保存版のIDと根拠IDを照合し、当時の入力で日経平均の説明へ答える経路を追加する。新しいニュース・価格を混ぜず、参照版と時点を私的履歴に保持する。不明・不整合・未来時点の参照はAI実行前に拒否する。従来の現在文脈の質問・個別銘柄の自動説明は維持する。
PR #362はRecovery/Product両証明と必須検査を通過し、main `f151781e68e2a48e38ba3f5030f656b2797071b8` へ通常の2親マージで反映した。関連API・暗号化復元・隔離ブラウザは確認済み。本番の新経路・私的履歴とiPhoneの受入は未完了。13.7には進まない。

### 13.5.76 保存版への質問を画面に接続
PR #362の保存版参照APIを、Todayの前回成功した説明・比較チャート直下の質問欄へ接続する。保存IDと根拠IDを渡し、現在の市場入力と当時の説明を混ぜない。生成中・失敗・更新待ちを区別して表示する。履歴の過去版は参照専用を維持。画面3ファイルの隔離Chromium390/1280px受入・lint/build証跡は既存のものを再利用し、13.5.76の配信・実画面は別途確認する。13.6全体未完了、13.7保留。

## 2026-09-14: 13.5.77本番到達点と13.6.0リリース条件

PR #364はmain `702fc1fce6f36253e118c43e81b5874bf30dcd52`へ通常の2親マージで反映し、
backendとPagesを13.5.77へ配信した。実ブラウザのSettingsで、表示バージョンが
`v13.5.77`だけであること、frontend/backendの同一build、ニュース取込HEALTHY、
保有・監視銘柄の重要変化を受け取る明示的な通知設定を確認した。

同じ本番起動で、費用制限を強制しない設定、費用台帳の復元3行、write-through、
再起動後の追加保存を公開診断で確認した。新しい市場説明は実際に
`gpt-6-astra`へ要求され、最初の不採用応答を一度だけ校正した後、同じ根拠に基づく
採用回答を保存した。生成、採用、保存と画面表示は別の状態として記録する。
この呼出しでは機能・要求モデル・実応答モデル・入出力token・推定費用を台帳へ残した。

PR #365はRecovery-only候補として全必須検査を通過し、main
`f7a16fe497f0c864de9d95e4495b84298cfb5522`へ通常の2親マージで反映した。
ブラウザを開いていない間も、保有・監視銘柄の重要変化通知候補と、保存済み銘柄説明の
期限更新をschedulerから実行する。私的保有情報を取得できない場合も、独立したSQ・ニュース
通知を止めない。通知本文には銘柄、名称、数量、取得価格を含めず、明示的に有効化した
既存購読だけを対象にする。通知の実装・検査と、所有者iPhoneへの実着信確認は区別する。

13.6.0は13.5.77と上記Recovery変更を統合する正式なProductリリースとする。
現在の実装には、Todayの一つの統合説明、前回からの変更、1/5/10/20営業日、
現在実績・類似局面・類似局面のその後・現在予測の4層比較、質問、保存履歴、
個別銘柄の市場・業種比較、市場内部、信用需給、日経レバ分解、VIX、海外投資家フロー、
CFTC円ポジション、SQを含む30日イベント、重要ニュース、Seven Signが接続されている。
表示された数字は計算結果、時点、対象期間、根拠IDと結び付け、LLMが割合を生成しない。

13.6.0本番受入では、backend/Pagesの同一バージョンとbuild、Todayの読む順序、
比較チャートの4系列、Seven Sign、重複しない重要ニュース、SQ・30日イベント、
個別銘柄と質問の根拠一致、Settingsの保存・復旧状態、実応答モデル、機能別費用台帳、
Recoveryの起動・暖機を確認する。iPhoneでは更新後の保存値維持と通知の実着信・遷移を
最終確認する。実着信を確認できるまで通知を本番確認済みとはしない。

公式指数と定義が一致するEPS/PER取得は、ローカルと本番の双方で供給元がHTTP 403を返し、
安定した実データ接続には至っていない。独立期間での予測追加効果も未検証である。
類似局面の頻度を予測確率や的中率として表示せず、BUYは構造的に無効のまま維持する。
Git履歴、既存ログ、バックアップ、外部記録には旧名称が残り、現行ソース・設定・保存形式・
配布物の外部ポリシー検査と履歴消去は別の状態である。履歴の強制書換えはしていない。

13.6.0の本番受入と総点検で停止する。13.7の費用・構成見直しは、オーナーが明示的に
再開するまで調査、実装、試作、PR、配信を行わない。

## 2026-09-14: 13.6.0本番受入と13.6.1台帳継続補修

13.6.0はmain `38ca70ad6f2fb7ef93b0cdc233af3733085e3673`へ反映した。
backendとPagesは同じバージョン・buildでREADYとなり、Pages run `34851485670`は、
PWA更新、モバイルToday、12件の実データスナップショット、共有分析エンジンを含む
公開受入を完了した。Today、個別銘柄、通知、Settingsの実ブラウザ確認も実施した。

同時刻の定期予測台帳では、15銘柄のGPT判断自体は本番で完了したが、Actionsの待機上限
240秒を約1分超えたため、その後の正本台帳追記が実行されなかった。13.6.1では観測上限を
420秒へ合わせ、外部AIの遅延・タイムアウトを正本の記録・採点から分離する。計算式、
判断条件、閾値、売買権限は変更しない。AI待機が完了しない場合も警告と要約を残す。

iPhoneの保存値維持、所有者文脈、閉じた状態での通知実着信・遷移は実機確認を要する。
これらと予測力の独立検証は、実装済み・本番配信済みとは区別する。13.7は引き続き、
オーナーの明示的な再開指示まで着手しない。
