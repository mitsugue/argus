# 13.6 分析・統合AIの追加受入条件

2026-09-12のオーナー追加指示。既存13.6の具体化であり、実装のやり直しや13.7着手ではない。
参考研究記録は製品外の匿名参照 `research-20260912-market-01` で追跡する。
研究動画の経験則・数値・将来予測を、検証済み事実や固定ルールに昇格させない。

## 現在の再利用・追加対象

| 項目 | 再利用できる実装・証跡 | 必要な追加・現在の限界 |
|---|---|---|
| 市場内部の広がり | Market Ledgerの市場区分別騰落数・母集団、既存breadth収集、relative_strength/rotation_map、D07の対業種・対指数反応 | Today・個別株・保有・統合AIの共通根拠へ接続。実際の業種集合と時点の照合。空配列の業種を取得済みとしない |
| 指数寄与と集中度 | 個別価格取得経路、銘柄分類 | 指数構成・適用時点のウェイト/除数/調整係数が必要。保有集中度や単純騰落率を指数寄与と呼ばない |
| 短期・中期 | 1/5/10/20営業日の比較計算、5/20日相対力、個別反応1/3/5日 | 対象・期間別の正式見立て・根拠・変更条件と会話/チャートを共通化。長期は数値検証状況を区別 |
| EPS更新と予測履歴 | 指数整合の評価尺度契約、過去比較4層、予測・結果の既存保存経路 | 実EPS/PER取得と旧/新予測の不変記録、入力改訂・理由・結果の追跡。現在の類似比較は価格形状・基準100の部分機能 |
| イベントの前後 | macro_results、macro_market_reaction、macro_event_analysis、公式イベントの改訂・保存処理 | 発表前予想の実在する出典・時点と、公式結果/改訂/市場反応/見立て変更を同一根拠へ接続。取得と予測力を分ける |
| 円の投機ポジション | 原典一覧・研究ベンチマークにCFTC参照あり | 円COTの残高取得・保存実装は未発見。公開原典の安定取得を調査する追加候補。規制ニュースのCFTC参照を残高接続済みとしない |
| ドル建て日経平均 | 価格比の計算、指数・為替取得の既存経路 | 現行chart-intelligenceの該当比率はETF価格を使う。現物日経平均のドル換算完了とは扱わず、入力商品・時刻を修正・照合する |

この表は2026-09-12時点のコード照合であり、各機能の本番受入完了を示さない。
信用残の買残/売残分解・制度/一般内訳は既存13.6 Worktreeの追加接続を再利用する。

## 共通の根拠と時間軸

見立ての識別には対象ID、商品種別、市場、通貨、セッション、価格基準、情報締切、
予測期間（開始・終了・営業日数）を含める。入力の出典・対象日・公表・実取得・改訂、
計算方式/版、根拠IDを保持する。Today、個別株、会話、チャートは同じ対象・期間の
同じ根拠IDを参照する。別画面で別の根拠なし作文を生成しない。

短期反発と数週間先の警戒は併存できる。異なる期間を一つの点数に加算しない。
同じ期間の文章・線・価格帯が異なる場合は不整合として検出する。年末等までの数値検証が
ない場合は条件付きシナリオとし、未検証の数値線・割合を作らない。会話上の仮定は
正式見立てのIDを参照する別記録とし、実測・実保有・正式予測履歴を変更しない。

過去局面の経過営業日と、SQ等のイベントからの営業日距離は別の特徴である。
同月日だけで類似認定せず、将来の結果を使って候補を選ばない。権利日等は既存の拡張候補を
維持し、今回すべてのイベント接続を必須へ拡大しない。

## 市場内部・業種・保有との関係

騰落数は市場区分と当時の対象集合、除外条件、期待銘柄数、取得数、有効比較数、欠測数を
伴う。同一期間・価格調整基準で上昇/下落/変わらずを計算する。一部の取得銘柄を市場全体と
呼ばず、同じ銘柄を追った期間比較と構成変更による差を区別する。

業種別は分類の出典・有効日、等ウェイト/時価総額等の集計方式、比較指数を明示する。
保有・監視銘柄の所属と実リターンを結び、市場が慎重でも業種が改善する理由を説明する。
NT倍率だけから高配当株全体の強さを推定しない。登録銘柄だけの観測は登録集合と表示する。

指数寄与は当該指数の正しい算出方式と当時の構成・調整係数を要する。
寄与度の単位と指数変動への合計整合を確認し、上昇側/下落側の集中を分ける。
相殺で小さくなった指数変化を分母にして集中度を誤表示しない。入力不足時は未算出とする。

## EPS更新・旧予測・結果

予測発行時の入力/仮定・指数EPS/PERの定義・当時の尺度変換・候補選択を凍結して保存する。
更新後は新しい予測IDと前版ID、変わった入力、変更理由を追加する。最新EPSで以前の線を
上書きしない。その後の結果はそれぞれの発行IDへ追記し、外れた予測も残す。

指数EPS、ETF、時価総額加重PER等を混在させない。特定PER/PBRを確定天井とせず、
採用条件・仮定・検証状態を示す。過去実績、過去のその後、現在の計算予測を区別し、
類似の理由だけでなく相違点と無効条件を示す。割合は計算側のみが出し、類似頻度・
検証済み予測確率・データ充足度を分ける。追加効果の未達を黙って対象外にしない。

## イベントの事前情報・結果・市場反応

指標IDには総合/コア、前年比/前月比、単位、季節調整の有無、対象期間を含める。
発表前予想は出典と実取得時刻を凍結し、公式結果、前回値、訂正前/後を改訂で残す。
予想未取得は未取得であり、発表後の値から予想・サプライズを逆算しない。

株価・金利・為替の反応は、対象商品と発表前後の観測窓・価格基準を付ける。
現物終値と夜間先物・CFD・ETFを同時点の矛盾として扱わない。先物は限月を含める。
見立て維持/変更の理由を、同じイベントと反応の根拠へ結ぶ。織り込み済みという説明にも
発表前予想・価格等を引用し、価格上昇だけで単一原因を断定しない。

## 円ポジションの限定候補

円だけを対象とし、既存経路または公開原典で安定取得できるかを確認する。
非商業部門等の取引者区分、先物のみ/オプション込み、取引所・契約コード・枚数単位を
固定し、買い・売り・スプレッド・ネット・前回差を保持する。TFFの区分とLegacyを混ぜない。
ネット買い越しは売り残ゼロを意味しない。対象日、公表日時、実取得時刻を別に持ち、
通常日程だけから実公表を断定しない。祝日・遅延・訂正があり得るため公表案内と照合する。
公表時点が確認できない履歴は取得時刻以降のみ利用し、当時の既知データとしない。

大規模な追加開発・有料契約が必要なら理由・費用・影響を台帳へ記し、保留候補とする。
この項目だけで13.6全体を止めず、全商品へ自動拡大しない。これは限定候補の評価であり、
13.7の契約削減・構成最適化ではない。既存の金額ゲート無効化指示を取り消すものでもない。

ドル換算は現物日経平均と同時点のUSD/JPY（円/米ドル）を使う。日付だけで時間差を無視せず、
合致した観測点・換算式・欠測を示す。ドル建ての上昇を海外投資家の注文の直接観測としない。

## 計算と説明に共通する禁止解釈

- 制度信用・一般信用・現引きを区別し、信用取引全部が一定月数後に市場売却されるとしない。
- 高値の日付だけから、現在残る建玉の取得日・将来の売却量を推定しない。
- 日次の空売り比率（取引集計）と売り建玉残高を混同しない。
- VIXの1日変化とMACDの交差・継続状態を区別する。
- 原典の個人名や派生識別子を製品へ再導入しない。原典本文/URLの研究保全は製品外で行う。

## 追加受入（すべて未受入から開始）

1. 実データで指数と業種・保有株の評価が異なる理由を、母集団と期間付きで説明する。
2. 短期反発と中期警戒を、同一対象の別期間としてチャートと説明で両立する。
3. EPS更新前後の予測ID・入力・変更理由・別々の結果を保存し、復元後も旧線が残る。
4. イベントの事前予想・公式結果/改訂・市場反応・見立て変更を通して追跡する。
5. Today/個別株/会話/チャートが同じ根拠ID・対象・期間を用いる。

取得成功、画面表示、計算検証、予測力検証、本番受入を別々に報告する。
最終報告に本番バージョン・コミットと未対応/未検証範囲を含める。
13.6の本番受入と既存合意の機能総点検を終えて停止し、13.7は明示再開まで保留する。

## 段階配信：市場説明の定期生成と円の建玉

公開市場の6項目説明をニュース収集から独立した定期処理へ接続する。ニュース解析が
長引く場合やメール未設定の場合も実行し、重複実行は抑制する。失敗・出力却下は再試行し、
成功した同じ根拠の説明を再利用する。却下理由を記録し、プロンプトと参照・数値・事実区分の
検証条件を一致させる。旧3項目の回答が保存されていても6項目の生成を止めない。

円だけを対象に、CFTC公式のLegacy Futures Only・非商業区分を既存収集経路へ追加する。
買い・売り・スプレッド・差引と前週差、対象日、実際の取得時刻を保持する。公表日時は
確認できなければ不明とし、週次報告を現在の建玉と呼ばない。既存市場台帳へ改訂を追記し、
同じ報告の再取得で初回取得時刻を上書きしない。Todayと公開市場AIは同じ保存済み報告を使う。
取得失敗時は前回分と更新失敗を表示する。新規契約や全商品対応は含まない。

この段階はローカル実装・関連検査の確認であり、本番の実取得・保存復旧・実GPT回答・
iPhone確認は別途記録する。取得・表示を予測力の証明とはしない。個別株・対話・保有文脈・
EPS予測の永続履歴・市場内部の比較・通知など、13.6全体の残要件を完了扱いにしない。
13.7は明示的な再開指示まで保留する。

## 追加実装中：見立てと計算の履歴

6項目の公開市場説明と当時の根拠、1・5・10・20営業日の計算結果を同じ記録に固定する。
最新の入力で古い線を書き換えず、新しい見立ては新しい内容IDを持つ。現在の実装対象は
公開市場の説明であり、保有情報や会話上の仮定を公開履歴へ混ぜない。

既存の永続領域内の追記専用データベースへ記録し、再起動時に前回の見立てを読み戻す。
履歴の閲覧は読み取り専用とし、現在のAI生成や保存を開始しない。対象営業日の実績終値を
取得したら、当時の計算値との差を別の結果記録へ追記する。結果の訂正も古い結果を残す。
日付・指数の種類・情報取得時刻・対象期間を照合し、発表後に作った予測を事前予測として採点しない。
単発の結果によるルール変更や、未検証割合の予測確率への昇格は行わない。

実装・関連検査済みの範囲と、本番の保存・再起動・画面・遠隔復旧は区別する。現時点では
永続領域のローカル読み戻しのみ検証済みで、遠隔バックアップへの接続・実復旧は未完了。
EPS/PERの実系列接続、市場内部の全体比較、個別株・保有・対話との共通根拠、通知も継続する。
予測線は既存の部分比較が出すものを保存する段階であり、分析全体や13.6の完成宣言ではない。

今回の接続では、説明の再利用判定に計算入力の内容も含める。同じ見出しでも価格・入力が
変われば再生成し、単なる読取り時刻の更新では呼び出しを増やさない。期間別の計算事実は
同じ根拠集合に渡すが、1・5・10・20営業日の判断を合算しない。
統合AIの数値・参照検証で回答が却下された場合は、同じ根拠で一度だけ修正を求め、
すべての検証を再適用する。修正も不合格なら未生成として扱い、最後の合格回答を保全する。
最初と修正の呼出し・費用・検証理由は別々に記録する。

円ポジションは本番で公式報告の実取得と内訳表示を確認済み。取得完了と永続保存中の状態を
分けて表示する。公表日時の原典確認、端末での受入、遠隔復旧と有効性検証は別の残件である。

### 遠隔履歴復旧の追加実装

既存の認証済み非公開保存先を使い、公開市場履歴の全記録・当時の計算・結果訂正を
内容ハッシュ付きの分割ファイルで保存する。取得不能を空データと見なさず、全ファイルの
照合後に内容IDで統合する。更新ポインタは比較交換で進め、別の起動環境の履歴を失わない。
古い遠隔記録が後から届いても、それを最新の見立てへ逆戻りさせない。

この追加分は実装・障害注入テスト済み。本番の遠隔書込み・読み戻し・コールド復旧は未確認。
画面上の遠隔保存先との内容照合と、本番復旧受入は別表示にする。認証情報が未設定、保存先が
不通、整合性が不一致の場合は未確認の範囲と最後の成功時刻を示し、サーバー内の履歴は残す。
128MiBの転送保護上限を超えた場合も履歴を間引かず、保存失敗として保全する。


## 市場内部の比較の追加実装

[指数・業種・銘柄比較の実装と受入状態](V13_6_MARKET_INTERNALS.md)を参照。
実データの取得、画面表示、予測力の検証を別項目とする。
