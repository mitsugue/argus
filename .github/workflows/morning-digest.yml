# ARGUS Morning Digest — the agent's daily push.
# Fetches /api/argus/daily-digest (rule-based, no LLM) every JP weekday morning
# and pushes the Japanese brief to ntfy.sh (zero-signup push relay).
#
# Setup (one-time, optional — the workflow no-ops gracefully without it):
#   1. Install the ntfy app (iOS/Android) and subscribe to a topic with a long
#      random name you invent, e.g. "argus-mitsugu-x7k2p9q4w1".
#   2. Repo → Settings → Secrets and variables → Actions → New repository secret
#      Name: NTFY_TOPIC / Value: that topic name.
# The topic name acts as the delivery address+password — keep it private.
# Digest content is market commentary only (no holdings, no personal data).

name: morning-digest

on:
  schedule:
    # GitHub's free cron is best-effort and has been observed running 2–3.5h
    # late. Strategy: fire EARLY at an odd minute (less queue contention), then
    # let ntfy's server-side "Delay" scheduling deliver at the exact target
    # time (08:30 / 22:00 JST). Market data doesn't change in the gap (US
    # close 05:00 JST, TSE close 15:30 JST).
    # 21:49 UTC Sun–Thu = 06:49 JST Mon–Fri → delivered 08:30 JST (TSE pre-open)
    - cron: '49 21 * * 0-4'
    # 10:49 UTC Mon–Fri = 19:49 JST → delivered 22:00 JST (NYSE pre-open)
    - cron: '49 10 * * 1-5'
  workflow_dispatch: {}

permissions: {}

jobs:
  push-digest:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Fetch digest (retry through Render cold start)
        id: digest
        run: |
          BE="https://argus-backend-3j2m.onrender.com"
          for i in 1 2 3 4 5 6; do
            if curl -sf --max-time 60 "$BE/api/argus/daily-digest" -o digest.json; then
              break
            fi
            echo "attempt $i failed (cold start?) — retrying in 20s"
            sleep 20
          done
          test -s digest.json
          python3 - <<'PY'
          import json
          d = json.load(open('digest.json'))
          text = d.get('textJa') or 'ARGUS digest unavailable'
          open('digest.txt', 'w').write(text)
          print(f"status={d.get('status')} date={d.get('dateJst')}")
          PY

      - name: Push to ntfy (skipped when NTFY_TOPIC secret is not set)
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: |
          if [ -z "$NTFY_TOPIC" ]; then
            echo "NTFY_TOPIC not configured — skipping push (digest fetched OK)."
            exit 0
          fi
          # Session detection + exact-time delivery. The 21:49 UTC run (even
          # delayed past midnight UTC) is the JP brief targeting 23:30 UTC
          # (=08:30 JST); the 10:49 UTC run is the US brief targeting 13:00 UTC
          # (=22:00 JST). If we're already past the target (heavy cron delay),
          # send immediately; otherwise ntfy holds the message server-side and
          # delivers it at the exact time. No Click header: tapping should show
          # the text, not jump to the app (user request 2026-06-11).
          python3 - <<'PY' > sched.env
          import datetime
          now = datetime.datetime.now(datetime.timezone.utc)
          h = now.hour
          if h >= 18 or h < 6:  # JP run window (21:49 UTC + up to ~6h delay)
              session = "JP open"
              target = now.replace(hour=23, minute=30, second=0, microsecond=0)
              if h < 6:  # delayed past midnight UTC — target was "yesterday 23:30"
                  target -= datetime.timedelta(days=1)
          else:
              session = "US open"
              target = now.replace(hour=13, minute=0, second=0, microsecond=0)
          margin = datetime.timedelta(seconds=120)
          delay = int(target.timestamp()) if target > now + margin else 0
          # Delivery time shown IN the title — iOS notification lists collapse
          # timestamps to "yesterday", so the JST send time must be explicit
          # (user request 2026-06-12).
          jst = datetime.timezone(datetime.timedelta(hours=9))
          shown = (target if delay else now).astimezone(jst)
          print(f'SESSION="{session}"')
          print(f"DELAY={delay}")
          print(f'SENTAT="{shown.strftime("%m/%d %H:%M")}"')
          PY
          . ./sched.env
          DELAY_HDR=()
          if [ "$DELAY" != "0" ]; then DELAY_HDR=(-H "Delay: $DELAY"); fi
          curl -sf --max-time 30 \
            -H "Title: ARGUS Digest - $SESSION ($SENTAT)" \
            -H "Tags: chart_with_upwards_trend" \
            "${DELAY_HDR[@]}" \
            --data-binary @digest.txt \
            "https://ntfy.sh/$NTFY_TOPIC" > /dev/null
          echo "digest pushed (session=$SESSION delay=$DELAY sentAt=$SENTAT)."
