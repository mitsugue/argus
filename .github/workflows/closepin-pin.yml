name: closepin-pin
on:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Feature retired. Historical records are preserved; no collection or scoring runs."
