import React from 'react';
import { PageShell, Placeholder } from './PageShell';

export const EventRadar: React.FC = () => (
  <PageShell crumb="04 · EVENTS" title="事象">
    <Placeholder note="FOMC / CPI / BOJ / earnings — エスカレーション式 (Phase 5)" />
  </PageShell>
);
