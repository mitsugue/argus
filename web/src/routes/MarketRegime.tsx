import React from 'react';
import { PageShell, Placeholder } from './PageShell';

export const MarketRegime: React.FC = () => (
  <PageShell crumb="03 · REGIME" title="相場">
    <Placeholder note="Regime タグ + SectorBlob 下層 (Phase 8)" />
  </PageShell>
);
