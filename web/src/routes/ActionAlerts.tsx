import React from 'react';
import { PageShell, Placeholder } from './PageShell';

export const ActionAlerts: React.FC = () => (
  <PageShell crumb="02 · ALERTS" title="通報">
    <Placeholder note="アセットクラス別 ActionAlert カード (Phase 4)" />
  </PageShell>
);
