import React from 'react';
import { PageShell, Placeholder } from './PageShell';

export const Watchlist: React.FC = () => (
  <PageShell crumb="05 · WATCH" title="監視">
    <Placeholder note="JP / US 個別株 (Phase 6)" />
  </PageShell>
);
